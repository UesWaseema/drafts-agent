# ─────────────────────────  requirements.txt  ─────────────────────────
# Core app
streamlit
python-dotenv
pandas
beautifulsoup4
tabulate

# Database drivers
mysql-connector-python
pymysql
psycopg2-binary

# AI / LLM stack
crewai              # last release that still runs on tenacity 8.x
langchain-community
scikit-learn
pydantic          # satisfies crewai ≥0.12

# Prompt-caching
litellm[redis]==1.73.0
redis

# ───────── version pins to stop back-tracking ─────────
# Instructor chain compatible with tenacity 8.x
instructor          # requires jiter 0.6.1 and tenacity 8.x
jiter

# Explicitly pin tenacity 8.x so every lib agrees
tenacity

# OpenTelemetry bundle (pulled in by Crewai & Streamlit)
opentelemetry-api==1.34.1
opentelemetry-sdk==1.34.1
opentelemetry-exporter-otlp-proto-grpc==1.34.1
opentelemetry-exporter-otlp-proto-http==1.34.1
opentelemetry-exporter-otlp-proto-common==1.34.1
opentelemetry-proto==1.34.1
opentelemetry-semantic-conventions==0.55b1
backoff==2.2.1
# ───────────────────────────────────────────────────────
celery[redis]
